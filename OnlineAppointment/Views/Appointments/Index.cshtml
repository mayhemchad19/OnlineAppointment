@model IEnumerable<OnlineAppointment.Models.Appointment>

@{
    ViewBag.Title = "Index";
}
<br />
<br />
<br />
<h2>Appointment List</h2>

<p>
    @Html.ActionLink("Set new Appointment", "Create")
</p>
<div class="col-md-12">

    <table class="table table-striped table-bordered col-md-12" id="example" align="center">
        <thead>
            <tr>
                <th>
                    Status
                </th>


                <th>
                   Date
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Slot.Time)
                </th>
                <th>
                   Customer Name
                </th>
                <th>
                    Service
                </th>
                @*<th></th>*@
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AppointmentState.AppointmentStatus)
                </td>


                <td>
                    @Html.DisplayFor(modelItem => item.AppointmentDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Slot.Time)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.FirstName)
                    @Html.DisplayFor(modelItem => item.User.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Product.ProductName)
                </td>
                @*<td>
            @Html.ActionLink("Approve", "Edit", new { id = item.AppointmentId }, new { onclick = "return confirm('Do you want to Approve this Appointment? ')" }) |
            @Html.ActionLink("Send Reminder", "Details", new { id = item.AppointmentId }, new { onclick = "alert('Reminder has been sent to their email');" }) |
            @Html.ActionLink("No Show", "Delete", new { id = item.AppointmentId })|
            @Html.ActionLink("Reschedule", "Resched", new { id = item.AppointmentId })
        </td>*@
            </tr>
            }

        </tbody>
    </table>

</div>
